/*\file FileProcessor.h
\brief Processes the files in the song and puts them into a file for the parser to put into a .db file*/
#pragma once

#include <sqlite3.h>
#include <filesystem>
#include <string>

namespace SJ
{
	/*\class FileProcessor.h
	\brief Process files in the song list, removing non 7k maps and creating a filepath to the .osu file*/
	class FileProcessor
	{

	};
}